
import streamlit as st

# Заготовленные задачи и мок-ответы
TASKS = {
    "Аналитика данных": """📊 Анализ продаж:
- В апреле наблюдается падение на 35% по сравнению с мартом.
- В мае и июне резкий рост: +100% за 2 месяца.
- Самые низкие продажи — в апреле.
📎 Общий вывод: Сезонный спад весной сменился активным ростом в летний период.""" ,

    "Анализ обращений": """🧠 Анализ обращения:
- Тональность: Негативная
- Категория: Баг
- Резюме: Пользователь не может войти в аккаунт и недоволен поддержкой.""" ,

    "HR-аналитика": """👩‍💼 HR-анализ:
- Жалобы: отсутствие онбординга, отсутствие руководителя, несогласованные задачи
- Предложения: назначить куратора, структурировать входные задачи""" ,

    "Бизнес-резюме": """📈 Бизнес-резюме:
Мелкий бизнес теряет позиции из-за высокой конкуренции и плохой поддержки. Необходима оптимизация SLA и усиление клиентского сервиса.""" ,

    "Генерация идей": """🧪 Идеи:
1. Кнопка отмены заказа в приложении.
2. Оптимизация загрузки изображений.
3. Функция избранного.
4. Push-уведомления о статусе доставки.
5. Интеграция быстрого чата."""
}

st.set_page_config(page_title="Prompt Trainer", page_icon="🧠")

st.title("🧠 Prompt Trainer (Без API)")
st.markdown("Тренируйся писать эффективные промпты под реальные задачи без использования GPT!")

task = st.selectbox("Выберите задачу", list(TASKS.keys()))

with st.expander("📌 Эталонный ответ от 'нейросети'"):
    st.markdown(TASKS[task])

user_prompt = st.text_area("✍️ Введите свой промпт под задачу", height=200)

if st.button("💾 Сохранить промпт"):
    with open("user_prompts_log.txt", "a", encoding="utf-8") as f:
        f.write(f"---\nЗадача: {task}\nПромпт: {user_prompt}\n\n")
    st.success("✅ Промпт сохранён!")
